%h2 Movies with director @director_movies[:director]
%table#movies.table.table-striped.col-md-12
  %thead
    %tr
      %th Movie Title
      %th Director
      %th Rating
      %th Release Date
      %th More Info
  %tbody
    - @director_movies[:movies].each do |movie|
      %tr
        %td
          = movie.title
        %td
          = movie.director
        %td
          = movie.rating
        %td
          = movie.release_date
        %td
          = link_to "More about #{movie.title}", movie_path(movie)
//= link_to 'Add new movie', new_movie_path, :class => 'btn btn-primary'
